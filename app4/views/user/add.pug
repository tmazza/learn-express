extends ../layout

block content
    form(method='post')

        p
            label(for='nome') Nome:
            if user
                input(name="nome", value=user.nome)
            else
                input(name="nome")

        p
            label(for='idade') Idade:
            if user
                input(name='idade', type='number', value=user.idade)
            else
                input(name='idade', type='number')
        p
            button(type='submit') Vai